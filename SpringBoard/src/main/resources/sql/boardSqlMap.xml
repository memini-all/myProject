<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.project.board">

	<!-- 글등록 -->
	<insert id="regist" parameterType="boardVO">
		INSERT INTO tbl_sboard (userno, title, content)
		VALUES(#{userno}, #{title}, #{content})
	</insert>
	
	<!-- 글목록 : 글 전체목록을 가져온다 > 추후 수정 -->
	<select id="list" resultType="boardVO">
	<![CDATA[
		SELECT 
		   brdno, userno, title, content, regdate, updatedate, deleteat, viewcnt 
		FROM 
		   tbl_sboard 
		WHERE brdno > 0 
		ORDER BY brdno DESC, regdate DESC
	]]>
	</select>

</mapper>